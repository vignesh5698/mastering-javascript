const powerOfAnyNumber = (number, powerNumber) => {
  return Math.pow(number, powerNumber);
}
number = 3;
powerNumber = 2;
powerOfNumber = powerOfAnyNumber(number, powerNumber);
console.log(powerOfNumber);